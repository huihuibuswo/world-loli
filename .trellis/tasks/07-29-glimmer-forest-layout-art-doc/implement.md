# 微光森林地图与贴图优化文档 - 执行计划

## 文档制作

- [x] 汇总剧情不变量、S0 视觉提示词、当前地图实现与资产审计结果。
- [x] 创建 `doc/剧情设计/开局/微光森林地图与内部贴图优化规范_V1.0.md`。
- [x] 写出演出视觉到俯视可玩地图的翻译原则和当前差距。
- [x] 写出地图分区、主路线、支路、剧情节点和相对布局示意。
- [x] 写出场景层级、深度排序、碰撞、遮挡、交互可读性和性能规则。
- [x] 编制资产矩阵与现有资产保留/调整/替换清单。
- [x] 编写统一风格母提示词、统一负面提示词和至少 9 组关键资产提示词。
- [x] 标记 Must / Should / Defer，并明确后续实现阶段需要校准的坐标与参数。

## 验证

- [x] 对照 `.trellis/tasks/07-27-opening-story/prd.md` 核对剧情不变量与信息边界。
- [x] 对照 `doc/剧情设计/开局/S0_冷开场_月痕_缺失贴图生成提示词.md` 核对颜色、雾向、古树和断月纹规则。
- [x] 对照 `WorldScene.ts` 和服务端地图对象核对入口、NPC、采集、证据、战斗与回村功能锚点。
- [x] 检查文档不存在 `TBD`、空章节、未闭合代码块和互相冲突的背景/透明要求。
- [x] 统计资产类别不少于 12 类、独立关键资产提示词不少于 9 组。
- [x] 完成一次对抗式审查，区分会阻碍后续制作的缺陷和可延期优化。

## 资产加工与接入

- [x] 新增确定性资产准备脚本，非破坏读取用户提供的 16 张原图。
- [x] 输出版本化美术源、public 运行时资源、manifest 和资产总览预览。
- [x] 清除棋盘格、洋红底和黑底，拆分图集并统一尺寸、命名与 Alpha。
- [x] 在 `PreloadScene.ts` 注册森林专属地表、路径、树木、障碍、雾和剧情视觉资源。
- [x] 在 `WorldScene.ts` 区分村庄/森林地表与道路，接入唯一核心古树、五种普通树、根系障碍、入口和证据贴图。
- [x] 接入两层东南向西北逆流雾和 `prefers-reduced-motion` 静态降级。
- [x] 使用现有可见位置解析避免采集物与 NPC、证据、传送点和障碍发生显示重叠。
- [x] 收紧月光空地 Alpha 软边，避免亮色贴纸边框。

## 实现验证

- [x] 59 个运行时资产清单与透明角点校验通过。
- [x] TypeScript 类型检查通过。
- [x] 客户端生产构建通过。
- [x] 既有时间系统测试 6/6 通过。
- [x] 将版本化生成资产与运行时镜像约定写入前端代码规范。
- [x] 新增 `023_opening_meet_chief_stage.sql`，修复开场阶段约束与服务状态机不一致。
- [x] 完整开场流程测试通过，数据库结构验证确认新增阶段合法且非法阶段仍被拒绝。
- [x] 浏览器完成森林角色移动、碰撞、透明画布通行、遮挡和露娜/月光百合站位验收。

## 风险文件与回滚点

- 正式规范位于 `doc/剧情设计/开局/微光森林地图与内部贴图优化规范_V1.0.md`。
- 资产变更限定在版本化 `glimmer-forest-v1` 美术源和 `public/assets/generated` 的森林子目录，用户原图保持不变。
- 代码变更限定在资产准备脚本、`PreloadScene.ts` 和 `WorldScene.ts` 的森林分支；村庄布局与资源保持独立。
- 回滚时先移除森林预加载与森林渲染分支，再移除版本化生成资产，不修改服务端数据。

## 部分2至5运行时实现

- [x] 新增 `024_glimmer_forest_regions.sql`，创建部分2至5、标记部分1并建立相邻双向门户。
- [x] 扩展 `verify_schema.sql` 与 API 流程测试，验证五区域身份、连通关系、出生点和非相邻跳转拒绝。
- [x] 扩展 `MapData.resource` 类型，加入 `region_key` 与 `region_name`。
- [x] 新建森林区域配置模块，集中五套地表、路线、碰撞、装饰、特效和传送标记选择。
- [x] 在 `PreloadScene.ts` 注册区域配置实际引用的版本化资产。
- [x] 改造 `WorldScene.ts` 按 `region_key` 绘制区域，同时保留部分1剧情对象、植物避障和 reduced-motion 行为。
- [x] 在 `GameShell.vue` 优先显示 `resource.region_name`，不修改数据库精确地图名。
- [x] 执行客户端 type-check/build、后端全量 27 项测试、schema 验证和五地图浏览器截图验收。
- [x] 对抗检查：未知 region_key 回退、出生点碰撞、反向门户连跳、前景遮挡、黑底/洋红边、透明叠层性能。

## 部分2：银雾溪谷定向优化

- [x] 按 V1.0 方案重排地表相位、宏观遮罩、主路与湿地环路。
- [x] 将单张大水带拆为 7 段水面，将溪岸、水草、桥、雾瀑和反光改为逐图显式布局，停用 `stream-bank-08`。
- [x] 以 12 段水体碰撞链作为通行权威，仅在两座桥下保留符合净宽的缺口。
- [x] 桥体固定为角色下层；小地图保留水面、桥和门户，隐藏水草、雾、反光和调试对象。
- [x] 增加地表相位、全资产显式布局、道路通行、桥中心线、桥侧阻挡、溪流中心线仅两个缺口和小地图可见性回归。
- [x] `test:forest` 19/19、TypeScript 类型检查、Vite 生产构建和 `git diff --check` 通过。

## 部分2：银雾溪谷拼接返工修正

- [x] 确认水纹源图为整张不透明材质，停止使用 7 张旋转 Image 拼水道。
- [x] 新增 `ForestWaterway`，使用全图 TileSprite + spline GeometryMask 绘制单一连续水道，并增加 20px 低 Alpha 岸缘过渡。
- [x] 停用不可拼接的 `stream-bank-01..06/08`，仅保留 `07/09` 桥下河床与 `10` 湿地汇入口。
- [x] 水道保持在道路之上、桥体与角色之下，并保留小地图导航表达。
- [x] Scene 关闭/销毁时显式释放 GeometryMask、隐藏 Graphics 与纹理引用。
- [x] 更新旧分段 ID、停用资产、runtime preload、小地图和配置校验回归。
- [x] 修订部分2贴图生成文档：废止不透明矩形河段和池塘式岸线，补充 W02 双向无缝材质、W03 独立局部件、W04 独立桥体的生成与拒收契约。
- [x] 新增部分2专用 V2.0 重新生成提示词文档，提供首批 13 张独立资产的文件名、完整提示词、生成顺序与拒收标准。

## 部分2：再生成素材确定性接入

- [x] 建立 W02/W03/W04/W05/W06 的源图映射，只采用合格或可确定性修复素材。
- [x] 新建非破坏 `glimmer-forest-regions-v3` 批次，保留旧 v2 和所有外部原图。
- [x] 两张水材质执行双向无缝化并输出 `2x2` 预览；运行时河形仍由 spline/mask 决定。
- [x] 两座桥、九张局部河岸/桥下河床、五张植物和逆流雾完成抠图、裁切、去紫边与 public 镜像。
- [x] 部分2运行时启用九张 W03 局部件、五张 W05 植物和新桥图；碰撞链、桥缺口和层级契约保持不变。
- [x] `test:forest` 20/20、TypeScript 类型检查、Vite 生产构建、manifest 镜像校验与 `git diff --check` 通过。

## 部分2：运行时布局体系化返工

- [x] 复核完整运行时合成图，确认问题来自矩形道路印章、河心贴花、错误语义素材和孤立植物撒点，而非单张素材清晰度。
- [x] 部分2主路与湿地支路改为 Phaser 双层低透明连续软带；不再重复或平铺非无缝湿土贴图。
- [x] 溪流收窄并降低岸缘 Alpha/外扩，保留原水域碰撞链与两处桥缺口。
- [x] W03 运行时收敛为 `01..04` 两组岸边局部结构；`05/06` 暗底风险素材、`07/08` 露边桥下矩形和 `09` 完整湿地场景全部停用。
- [x] 两座桥缩放并校正跨河角度，直接覆盖连续水面；四个矩形湿地反光全部停用。
- [x] 五张植物按统一视觉尺度重组为两处岸边群落，避开道路与桥头安全区。
- [x] 新增连续路径仅限部分2、岸线法向偏移、错误素材停用、桥河角度与植物群落回归；`test:forest` 22/22、非增量 TypeScript 检查、Vite 生产构建和 `git diff --check` 通过。

## 部分2：全图构图闭环修正

- [x] 以 2048×2048 全图合成为视觉权威，确认上一轮仍缺少谷地边界、生态重复语法和自然河宽变化。
- [x] 使用共享连续树墙建立南北林界，东西两侧普通树改为遮叠林团，并为可见树基补齐碰撞与传送安全区校验。
- [x] 河道中心线改为确定性弯曲，宽度使用低频正弦变化；岸线拆为柔和湿土外带、深色河床内带和水纹材质三层。
- [x] 河岸素材只复用同一种卵石岸基，七株植物组成三处重复生态群落；不再混用鹅卵石路、泥滩和矩形岸块。
- [x] 地表双重平铺 Alpha 收敛并加强宏观遮罩；两条路径继续使用连续软带，不恢复贴图印章。
- [x] 将“单图/接触表不能替代全图构图验收”写入生成资产规范，并要求全图与玩法尺度裁切双重检查。
- [x] `test:forest` 23/23、非增量 TypeScript 检查、独立 QA 目录 Vite 生产构建和 `git diff --check` 通过。

## 部分2：贴图与地表融合修正

- [x] 确认贴图感来自素材自带完整地块底边、区域间色温/明度差和缺少接触过渡，停止继续依赖坐标与透明度微调。
- [x] 资产脚本新增树墙底边及左右端 Alpha 羽化，并提供只生成该资产的窄模式，避免重写被占用的完整美术批次。
- [x] 运行时加入素材 tint、地表三圈软融合斑块、河岸三级低 Alpha 过渡与河水材质统一调色。
- [x] 树墙、林团、岸石、植物与桥统一压暗降饱和；河水降低亮度和 Alpha，消除浅灰覆盖层观感。
- [x] 新羽化资产的 art-source/public 镜像哈希一致，V9 全图与玩法尺度裁切完成视觉复核。

## 部分2：桥头岸线遮挡修正

- [x] 确认桥体默认深度 `-6` 高于普通岸石 `-6.6`，导致四个桥端完整暴露并产生贴纸感。
- [x] 为浅滩桥与主桥各增加两处桥头软融合层和岸基盖片，固定层级为河水/河床 -> 桥体 -> 软融合 -> 岸基盖片。
- [x] 桥头盖片仅参与视觉合成，隐藏于小地图且不增加碰撞，不改变既有桥缺口和通行坐标。
- [x] 增加四桥头存在性、深度、端点距离、无碰撞和小地图隐藏回归。
- [x] 输出 V10 全图、浅滩桥裁切和主桥裁切，逐端确认桥体进入河岸下方。
- [x] `test:forest` 24/24、独立 tsbuildinfo 类型检查、Vite runner 生产构建和 `git diff --check` 通过。

## 部分2：桥侧碰撞与道路断口修正

- [x] 确认半透明水面仍会显示桥下完整道路软带，造成红框区域出现错误道路贴图，并非单纯桥头岸基坐标问题。
- [x] 为两条连续路径增加桥中心视觉断口，保留逻辑动线与小地图表达，但停止在水面和桥体下绘制道路。
- [x] 将浅滩桥岸基盖片压缩为更接近河岸的比例，并向桥体内侧微调，使桥端埋入岸基而非落在碎石道路上。
- [x] 收紧两座桥相邻的四块水域碰撞体；未新增桥头碰撞体，桥头安全区、桥中心线和两个连续通行缺口保持有效。
- [x] 增加路径视觉断口和桥侧碰撞边界回归，V11 两座桥玩法尺度裁切完成视觉复核。
- [x] `test:forest` 24/24、独立 tsbuildinfo 类型检查、Vite runner 生产构建和 `git diff --check` 通过。
